# Бүлэг 1: Python програмчлалын танилцуулга

Эх сурвалж: `src/data/chapters/chapter-1.ts` доторх Chapter 1-ийн сургалтын агуулга.

## Материалын хэрэглээ

- Зориулалт: багшийн лекц, ангийн дадлага, богино үнэлгээ
- Сэдэв: Python-ийн анхны командууд, функц, аргумент, командын дараалал
- Нийт хугацаа: ойролцоогоор 90 минут
- Хэрэглэх хэлбэр: эхлээд багш үндсэн ойлголтыг тайлбарлаж, дараа нь сурагчид Level 1-7 даалгаврыг платформ дээр гүйцэтгэнэ.

## Хичээлийн зорилго

Энэ бүлгээр сурагчид Codingforkids платформ дээр Python код бичиж, тоглоомын дүрийг хөдөлгөх, объекттой харилцах, функц ашиглах үндсэн ойлголтыг эзэмшинэ. Хичээлийн төгсгөлд сурагчид командын дараалал, функц, аргумент, код ажиллуулах үйл явцыг практик даалгавраар хэрэглэж чаддаг болно.

## Суралцах үр дүн

Хичээлийн дараа сурагч дараах чадвартай болсон байна:

- Code Editor дээр Python команд бичих
- `Run` товч ашиглан кодоо ажиллуулах
- `player` модулийн функцуудыг зөв дуудах
- Командууд дээрээс доош дарааллаар ажилладаг гэдгийг тайлбарлах
- `move_forward()`, `turn_left()`, `turn_right()` функцуудыг ашиглан дүрийг хөдөлгөх
- Функцийн аргумент ашиглан олон алхам хийх
- `push()`, `speak()`, `build()` функцуудыг тохирох нөхцөлд хэрэглэх
- Өмнө сурсан функцуудыг нэгтгэн богино шийдэл бичих

## Урьдчилсан шаардлага

- Компьютер ашиглах суурь чадвар
- Keyboard ашиглан текст бичих чадвар
- Codingforkids платформын Code Editor хэсэгт нэвтрэх боломж

## Гол ойлголтууд

| Ойлголт | Тайлбар | Жишээ |
| --- | --- | --- |
| Модуль | Тодорхой зорилготой командуудын багц | `player` |
| Функц | Тодорхой үйлдэл хийдэг команд | `move_forward()` |
| Функц дуудах | Модуль, цэг, функцийн нэр, хаалт бичих | `player.move_forward()` |
| Аргумент | Функцэд дамжуулж буй утга | `player.move_forward(3)` |
| Дараалал | Код мөр бүр дээрээс доошоо ажиллана | эхлээд алхах, дараа нь эргэх |
| Зорилго | Түвшний төгсгөлд байгаа од/гарц | од руу хүрэх |

## Ашиглах командууд

```python
player.move_forward()
player.move_forward(3)
player.turn_left()
player.turn_right()
player.push()
player.speak('Hello')
player.build('bridge')
```

## Хичээлийн бүтэц

| Хэсэг | Хугацаа | Агуулга |
| --- | ---: | --- |
| Нээлт | 5 минут | Python, Codingforkids платформ, Code Editor-ийн зорилгыг танилцуулах |
| Level 1 | 10 минут | Анхны команд бичиж дүрийг нэг алхам урагшлуулах |
| Level 2 | 15 минут | Функц, `player` модуль, хөдөлгөөний командууд |
| Level 3 | 15 минут | Функцийн аргумент ашиглан олон алхам хийх |
| Level 4 | 10 минут | Саадыг `push()` функцээр түлхэх |
| Level 5 | 10 минут | `speak()` функцээр дэлгэц дээр үг хэлүүлэх |
| Level 6 | 15 минут | Мод цуглуулж `build('bridge')` ашиглан гүүр барих |
| Level 7 | 15 минут | Сурсан бүх командуудаа нэгтгэн хэрэглэх |
| Дүгнэлт | 5 минут | Асуулт, богино шалгалт, гэрийн даалгавар |

## Багшийн тайлбар

### 1. Python болон платформын танилцуулга

Python нь тоглоом, веб апп, автоматжуулалт, дата боловсруулалт зэрэг олон төрлийн програм бүтээхэд ашиглагддаг програмчлалын хэл юм. Энэ хичээлд сурагчид Python-ийг тоглоомын орчинд туршиж үзнэ.

Сурагчдад дараах санааг ойлгуулна:

- Код бол компьютерт өгөх нарийн заавар юм.
- Тоглоомын дүр зөвхөн бичсэн командын дагуу хөдөлнө.
- Алдаа гарвал кодоо засаж дахин ажиллуулж болно.

### 2. Code Editor ба Run товч

Сурагчид Code Editor дээр команд бичээд `Run` товч дарж үр дүнг харна.

Жишээ:

```python
player.move_forward()
```

Энэ команд дүрийг нэг алхам урагшлуулна. Эндээс сурагчид код бичих, ажиллуулах, үр дүнг ажиглах үндсэн давталтыг сурна.

### 3. Функц ба командын дараалал

Функц нь тодорхой үйлдэл хийдэг кодын команд юм. `player` модуль нь тоглоомын дүртэй харилцахад ашиглагдана.

Функц дуудах хэлбэр:

```python
player.function_name()
```

Жишээ:

```python
player.move_forward()
player.turn_left()
player.move_forward()
```

Эдгээр командууд дээрээс доошоо нэг нэгээрээ ажиллана. Тиймээс дараалал өөрчлөгдвөл дүрийн хөдөлгөөн өөр болно.

## Түвшин бүрийн материал

### Level 1: Starting Out

Зорилго: Code Editor дээр анхны кодоо бичиж, дүрийг гарц руу хүргэх.

Үндсэн команд:

```python
player.move_forward()
```

Багшийн чиглүүлэг:

- Code Editor хаана байгааг зааж өгнө.
- Сурагчдаар командыг яг бичүүлнэ.
- `Run` дарж дүр нэг алхам хөдөлж байгааг ажиглуулна.

Асуух асуулт:

- Энэ команд ямар үйлдэл хийсэн бэ?
- Хэрэв код бичээд `Run` дарахгүй бол юу болох вэ?

### Level 2: Movement with Functions

Зорилго: Ойн замаар явж, гарц руу хүрэх.

Шинэ ойлголт:

- Функц
- `player` модуль
- Командын дараалал

Ашиглах командууд:

```python
player.move_forward()
player.turn_left()
player.turn_right()
```

Багшийн чиглүүлэг:

- Дүр хаашаа харж байгааг эхлээд ажиглуулна.
- Алхам бүрийг кодын мөртэй холбож тайлбарлуулна.
- Цагаан `X` нь эхлэх байрлал, од нь зорилго гэдгийг сануулна.

Дасгал:

Сурагч бүр замаа эхлээд цаасан дээр эсвэл амаар төлөвлөөд дараа нь код бичнэ.

### Level 3: Collecting Items

Зорилго: Газрын зураг дээрх дөрвөн жимсийг цуглуулах.

Шинэ ойлголт:

- Функцийн аргумент
- Нэг командыг илүү үр ашигтай ашиглах

Жишээ:

```python
player.move_forward(3)
```

Энэ команд дүрийг гурван алхам урагшлуулна. `3` нь функцэд дамжуулж буй аргумент юм.

Багшийн чиглүүлэг:

- `player.move_forward()` болон `player.move_forward(3)` хоёрын ялгааг харьцуулна.
- Олон удаа ижил мөр бичихийн оронд аргумент ашиглаж болохыг тайлбарлана.

Асуух асуулт:

- `player.move_forward(5)` гэж бичвэл юу болох вэ?
- Аргумент хаана бичигдэж байна вэ?

### Level 4: Pushing Objects

Зорилго: Замд саад болж буй чулуунуудыг түлхэж, гарц руу хүрэх.

Шинэ команд:

```python
player.push()
```

Багшийн чиглүүлэг:

- Дүр чулуу руу харсан үед л түлхэх боломжтойг онцолно.
- Хөдөлгөөн, эргэлт, түлхэх үйлдлийн дарааллыг сурагчдаар тайлбарлуулна.

Алдаа засах жишээ:

- Хэрэв дүр чулуу түлхэхгүй бол эхлээд зөв чиглэл рүү харж байгаа эсэхийг шалгана.
- `push` гэж бичээд хаалт орхисон эсэхийг шалгана.

### Level 5: Printing on Screen

Зорилго: Гурван `X` тэмдэг дээр очиж `Hello` гэж хэлэх.

Шинэ команд:

```python
player.speak('Hello')
```

Багшийн чиглүүлэг:

- Текстийг хашилтын дотор бичдэг гэдгийг тайлбарлана.
- `Hello` үгийг өөрчлөхөд дэлгэц дээр гарах текст өөрчлөгдөж болохыг харуулна.

Асуух асуулт:

- Яагаад `Hello` үгийг хашилтанд бичсэн бэ?
- `player.speak('Hi')` гэж бичвэл ямар ялгаа гарах вэ?

### Level 6: Building Objects

Зорилго: Мод цуглуулж, гүүр барин гарц руу хүрэх.

Шинэ команд:

```python
player.build('bridge')
```

Багшийн чиглүүлэг:

- Модны дээгүүр алхахад мод цугларна.
- Дэлгэцийн баруун дээд буланд хэдэн мод цуглуулсныг харж болно.
- 4 мод цуглуулсны дараа `X` тэмдэг дээр очиж гүүр барина.
- `bridge` үгийг яг зөв бичих шаардлагатайг анхааруулна.

Алдаа засах жишээ:

- `player.build(bridge)` гэж бичсэн бол `bridge` текстийг хашилтанд хийх хэрэгтэй.
- Мод дутуу байвал эхлээд шаардлагатай модоо цуглуулна.

### Level 7: Apply What You've Learned

Зорилго: Чулуу түлхэж, мод цуглуулж, гүүр барин гарц руу хүрэх.

Энэ түвшинд өмнөх бүх ойлголт нийлнэ:

- Дүрийг хөдөлгөх
- Зөв чиглэл рүү эргэх
- Саад түлхэх
- Мод цуглуулах
- Гүүр барих
- Командын дарааллыг төлөвлөх

Санах командууд:

```python
player.push()
player.build('bridge')
```

Багшийн чиглүүлэг:

- Сурагчдаар эхлээд газрын зургийг ажиглуулна.
- "Эхлээд юу хийх вэ?", "Дараа нь ямар саад байна вэ?", "Гүүр барихын өмнө юу хэрэгтэй вэ?" гэж асууна.
- Шууд код бичүүлэхээс өмнө богино төлөвлөгөө гаргуулна.

## Сурагчийн бие даалтын даалгавар

1. `player.move_forward()` командыг 3 удаа бичиж ажиллуул. Дараа нь ижил үйлдлийг `player.move_forward(3)` гэж бичээд харьцуул.
2. Дүрийг хоёр удаа урагшлуулж, баруун тийш эргүүлээд дахин нэг алхам явуулах код бич.
3. `player.speak('Hello')` командыг өөр үгээр сольж турш.
4. `player.build('bridge')` командын `bridge` үгийг буруу бичвэл юу болохыг ажиглаад тайлбарла.
5. Level 7 дээр эхлээд төлөвлөгөө бичээд дараа нь кодоо бич.

## Нийтлэг алдаа ба засах арга

| Алдаа | Шалтгаан | Засах арга |
| --- | --- | --- |
| `player.move_forward` гэж бичсэн | Хаалт дутуу | `player.move_forward()` гэж бичих |
| `player.moveforward()` гэж бичсэн | Функцийн нэр буруу | `move_forward` дотор доогуур зураас ашиглах |
| `player.speak(Hello)` гэж бичсэн | Текст хашилтгүй | `player.speak('Hello')` гэж бичих |
| `player.build(bridge)` гэж бичсэн | Аргумент текст тул хашилт хэрэгтэй | `player.build('bridge')` гэж бичих |
| Чулуу түлхэгдэхгүй байна | Дүр чулуу руу хараагүй | Эхлээд `turn_left()` эсвэл `turn_right()` ашиглаж чиглэлээ тааруулах |
| Дүр буруу газар очиж байна | Командын дараалал буруу | Кодоо мөр мөрөөр нь уншиж, газрын зурагтай харьцуулах |

## Богино шалгалт

1. `player.move_forward()` команд ямар үйлдэл хийдэг вэ?
2. `player.turn_left()` болон `player.turn_right()` хоёрын ялгаа юу вэ?
3. Функцийн аргумент гэж юу вэ?
4. `player.move_forward(4)` команд хэдэн алхам хийлгэх вэ?
5. Дэлгэц дээр `Hello` гэж хэлүүлэхийн тулд ямар код бичих вэ?
6. Гүүр барихын тулд ямар код бичих вэ?
7. Яагаад командын дараалал чухал вэ?

## Хариултын түлхүүр

1. Дүрийг нэг алхам урагшлуулна.
2. Нэг нь зүүн тийш, нөгөө нь баруун тийш эргүүлнэ.
3. Функцийн үйлдэлд нөлөөлөхөөр хаалт дотор дамжуулж буй утга.
4. Дөрвөн алхам.
5. `player.speak('Hello')`
6. `player.build('bridge')`
7. Код дээрээс доошоо ажилладаг тул дараалал өөрчлөгдвөл үр дүн өөр болно.

## Гэрийн даалгавар

Өөрийн үгээр дараах асуултад хариулж бич:

- Функц гэж юу вэ?
- Аргумент гэж юу вэ?
- `player.move_forward()` болон `player.move_forward(3)` хоёрын ялгаа юу вэ?
- Level 7-г шийдэхдээ ямар дарааллаар бодсон бэ?

Нэмэлтээр сурагч өөрийн дуртай 5 командын дарааллыг зохиож, команд бүр ямар үйлдэл хийхийг тайлбарлана.

## Багшийн тэмдэглэл

- Энэ бүлэгт хурднаас илүү зөв дараалал, зөв бичлэг, логик төлөвлөлтийг үнэлнэ.
- Сурагч алдаа гаргавал шууд хариу хэлэхээс өмнө кодын мөр бүрийг газрын зурагтай харьцуулуулна.
- "Юу болох ёстой байсан бэ?", "Код яг юу хийж байна вэ?" гэсэн хоёр асуултыг тогтмол ашиглавал сурагч өөрөө алдаагаа олоход тусална.
